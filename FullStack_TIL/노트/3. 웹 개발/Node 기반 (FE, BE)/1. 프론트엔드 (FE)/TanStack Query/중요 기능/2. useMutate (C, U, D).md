마찬가지로 
1) isPending
2) error
3) 캐시 기능
을 제공한다.

api를 불러옴과 동시에 캐시를 수정해주는 데,

api로 값을 받아온다고 해도 stale time 이내라면 refresh가 일어나지 않기 때문에, 
(캐시 값이 바뀌지 않기 때문에) UI에 변화가 생기지 않는다.

```
refresh (새로고침) : 
	=> TanStack Query의 특징 최신 데이터가 컴포넌트에 영향을 주는 기준
```


사용 예시) - 이 코드 만으로는 UI(컴포넌트)에 변화가 없다.
```jsx
const uploadPostMutation = useMutation({
  mutationFn: (newPost) => uploadPost(newPost),
});

const handleSubmit = (e) => {
  e.preventDefault();
  const newPost = { username: 'codeit', content };
  uploadPostMutation.mutate(newPost);
  setContent('');
};
```

### invalidateQuries() 함수 & onSuccess 콜백 함수
: 성공 시 자동 refresh 하는 로직 짜기

이때 refresh를 해결해주는 함수가 `invalidateQueries()` 함수다.
캐시에 있는 데이터가 stale time 이 지났는지 상관없이, stale 상태로 만든다.
stale상태가 된 데이터는 자동으로 refresh되며 UI에 변화를 준다.

onSuccess는 useMutation에 들어가는 인자 중 하나로, mutate 성공 시 실행할 콜백 함수를 받는다. 여기에 `invalidateQueries()`를 넣어 주면 api를 실행한 결과를 바로 캐시에 반영하고, UI에 변화가 생긴다.

onError는 에러 발생 시 실행할 코드를 넣는 곳이다. (에러메세지 표시 등에 사용)

```jsx
const queryClient = useQueryClient();

// ...

const uploadPostMutation = useMutation({
  mutationFn: (newPost) => uploadPost(newPost),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['posts'] });
  },
  onError: () => {
  }
});
```

### +) refresh한 쿼리가 useInfiniteQuery의 쿼리면

useInfiniteQuery면 복수의 데이터를 받아오고, 페이지 별로 캐시에 저장이 되는 데, api를 refresh할 경우 어떻게 될까? 뭔가 받았던 페이지를 모두 삭제하고 다시 페이지를 받을 것 같지만, 실제로는 모든 페이지의 표시를 유지시키고 정보를 갱신한다. 

![[Pasted image 20260113151435.png]]

어윤수 기사님은 `페이지[1]`의  0번째 요소에 해당하는데, 좋아요를 누르면 onSuccess의 invalidateQueries()로 정보가 갱신되어 다시 받아진다. 하지만 `페이지[0]`의 내용이 사라지는 것이 아니고 그대로 유지되며(변경이 있으면 갱신도 되며), 커서(Cursor)가 가리키는 위치(id)도 그대로 유지된다.

Tanstack Query와 useMutation()의 기능은 생각보다 많이 강력했다. 