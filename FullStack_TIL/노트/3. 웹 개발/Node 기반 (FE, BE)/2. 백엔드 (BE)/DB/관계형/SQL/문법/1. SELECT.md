
# `SELECT * FROM`

1) 데이터의 모든 열을 출력하소 싶을 때 사용 (열 이름, 데이터 상태 확인)
2) 데이터가 클지도 모르니 LIMIT 구문을 함께 쓸 것

FROM은 검색할 테이블
SELECT는 테이블 안에서 무엇을 선택할 것인지인데 보통 `*(all)`로 둔다.

```sql
SELECT * FROM customers
# 고객 테이블에서 모든 컬럼 검색

