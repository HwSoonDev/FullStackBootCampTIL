
어떤 열을 기준으로 정렬(내림차순, 오름차순)할 것인지

```sql
SELECT 출력하고 싶은 결과
FROM 테이블
WHERE 조건문
ORDER BY 열(컬럼) 이름 ASC(DESC)
```

1) 오름차순 = ASC, 내림차순 = DESC 
2) 여러 개의 컬럼을 기준으로 삼고 싶을 때는 우선순위 순으로 적고 콤마로 구분
   (e.g.) 

   ```sql
	ORDER BY  고객 가입일 DESC, 고객 이름 ASC
   ```
3) (기본 값은 오름차순이고 생략 가능)


