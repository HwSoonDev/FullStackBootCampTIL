
2초마다 주식 거래 차트를 갱신하고 싶을 때,
HTTP 요청을 2초마다 서버에 보내는 방법도 있을 것이다. (서버는 선 톡을 안한다.)

하지만 이건 너무 비효율적이고, 그냥 서버가 2초마다 데이터를 보내줬으면 하는 생각이 든다.

이 때 두가지 방법이 있는데, 그 중 첫번째가 SSE다.

기존 HTTP 통신은 단발성 통신으로 요청과 응답이 한번씩만 이루어진다.
그런데 SSE 방식은 HTTP 통신의 연결을 한번의 응답으로 종료하지 않고 유지시키는 방식을 사용한다.

