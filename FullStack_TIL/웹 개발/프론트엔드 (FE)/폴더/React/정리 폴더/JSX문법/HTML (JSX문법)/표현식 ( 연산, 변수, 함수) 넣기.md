```JSX
root.render (
  <>
    <h1>{product + ' ' + model} 주문하기</h1>
    <img src={imageUrl} alt="제품 사진" />
    <button onClick={handleClick}>확인</button>
  </>,
  document.getElementById('root')
)
```
JSX에서 표현식을 html 태그 속에 넣고 싶을 때는 {}중괄호를 사용한다.